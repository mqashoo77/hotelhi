<!-- activity_layout.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:padding="8dp">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewBooking"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/booking_card" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:background="?attr/colorPrimary"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageButton

            android:id="@+id/bookingHomeNavigationBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/colorPrimary"
            android:padding="5dp"
            android:scaleType="fitCenter"
            android:src="@drawable/icons8_home_50__2_" />

        <ImageButton
            android:id="@+id/bookingSearchNavigationBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/colorPrimary"
            android:padding="5dp"
            android:scaleType="fitCenter"
            android:src="@drawable/icons8_search_50__1_" />

        <ImageButton
            android:id="@+id/bookingRegisterNavigationBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_margin="0dp"
            android:layout_weight="1"
            android:background="?attr/colorPrimary"
            android:padding="5dp"
            android:scaleType="fitCenter"
            android:src="@drawable/icons8_register_50__1_" />

    </LinearLayout>

</RelativeLayout>
